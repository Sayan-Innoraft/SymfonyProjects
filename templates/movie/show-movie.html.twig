{% extends 'base.html.twig' %}

{% block body %}
	<div  class="container text-center">
		<h1>{{ movie.title }}</h1>
		<h4>{{ movie.year }}</h4>
		<img width="800px" class="rounded mx-auto d-block"
		     src="{{ movie.thumbnail }}" alt="{{ movie.thumbnail }}">
		<h5 class="mt-3">{{ movie.description }}</h5>
	</div>
{% endblock %}
